extends ../../layouts/default.pug

include ../../mixins/table-tree.pug

block main
  h1 Danh mục công việc

  .card.mb-3
    .card-header Danh sách
      .card-body
        .row  
          .col-8 
          .col-4 
            a(
              href=`${prefixAdmin}/projects-category/create`
              class="btn btn-outline-primary"
            ) + Thêm mới
  
        table(
          class = "table table-hover table-sm"
          checkbox-multi
        )
          thead
            tr
              th 
                input(type="checkbox" name="checkall")
              th STT
              th Hình ảnh
              th Tiêu đề
              th Vị trí
              th Trạng thái
              th Hành động

          tbody
            +table-tree(records)

  //- _id
  //- title 
  //- description 
  //- thumbnail 
  //- status 
  //- position
  //- deleted 
  //- deletedAt 
  //- createdAt  
  //- updatedAt  
  //- slug
